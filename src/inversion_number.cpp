#include <iostream>
#include "../include/fastinversion/inversion_number.h"


int count_inversions(int arr[], int len) {
    for (int i = 0; i < len; i++) {
        std::cout << arr[i] << ' ';
    }

    return 0;
}